# QuestBoard MCP server dependencies
# Install: pip install -r requirements.txt

mcp>=1.0.0
# Stellar Python SDK (Soroban). >=12 required for the Protocol 23 Soroban RPC
# API used here (prepare_transaction, scval.to_enum, v3/v4 tx meta). Verified with 14.1.0.
stellar-sdk>=12.0.0
requests>=2.31.0